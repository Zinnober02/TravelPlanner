# 服务器配置
server:
  port: 8080
  servlet:
    context-path: 
    session:
      timeout: 86400s

# Spring配置
spring:
  application:
    name: travel-planner
  main:
    allow-bean-definition-overriding: true
  # 静态资源配置
  web:
    resources:
      static-locations: classpath:/static/
  # Spring MVC配置
  mvc:
    static-path-pattern: /static/**
    welcome-page-url: /static/login.html
  # 数据库配置
  datasource:
    url: jdbc:postgresql://rm-cn-u1p4isuws00055vo.rwlb.rds.aliyuncs.com:5432/travel_planner
    username: dms_user_f928f10
    password: User123456
    driver-class-name: org.postgresql.Driver
  # JPA配置
  jpa:
    hibernate:
      ddl-auto: validate  # 仅验证模式不修改
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
        dialect: org.hibernate.dialect.PostgreSQLDialect

# 日志配置
logging:
  level:
    root: INFO
    com.travelplanner: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# 用户认证配置
auth:
  token-header: Authorization
  token-prefix: "Bearer "
  token-expire: 86400000

# 讯飞语音识别配置
xunfei:
  app-id: ea4f4a9d
  api-secret: OTMxMjU1MmE2NmI3MmUyNmQxNjJiNWRi
  api-key: 66ed0cadb85577717f41d6bd2b282dcc
  host: https://iat-api.xfyun.cn/v2/iat